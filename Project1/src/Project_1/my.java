package Project_1;

import terminalEditor.Color;

public class my {

    public static  void main(String[] args ) {
        myWindow window = myWindow.GetWindow();
        window.render();





 /*
MyTerminal terminal = MyTerminal.show();
terminal.clearTerminalWindow();
terminal.moveCursorTo(20,20);
terminal.setForegroundColour(Color.GREEN);
terminal.writeLineToTerminal("hello");
*/

    }
}
